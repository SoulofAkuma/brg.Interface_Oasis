<InterfaceOasis>
	<Groups>
		<Group requiresKeys="name,id" requiresValues="regexname,regexid">
			<Listeners>
				<Listener requiresKeys="log,port,name,id" requiresValues="regexbool,regexport,regexname,regexid"/>
			</Listeners>
			<Responders>
				<Responder requiresKeys="log,name,id" requiresValues="regexbool,regexname,regexid">
					<Header requiresKeys="url" optionalKeys="userAgent,contentType,customArgs" requiresValues="regexid" optionalValues="regexid,regexid,regexidlist"/>
					<Body requiresKeys="constants,seperator" requiresValues="regexidlist,string"/>
				</Responder>
			</Responders>
		</Group>
	</Groups>
	<Triggers>

	</Triggers>
	<Parsers>
		<Parser requiresKeys="name,id,order" requiresValues="regexname,regexid,regexidlist">
			<Rule requiresKeys="type,id" requiresValues="ruletype,regexid"/>
			<Rule type="parser.Cut" requiresKeys="id,find,n,keep,regex,reEval" requiresValues="regexid,string,int,boolean,boolean,boolean"/>
			<Rule type="parser.Discard" requiresKeys="id,find,inverted,regex,flags" requiresValues="regexid,string,boolean,boolean,stringarray"/>
			<Rule type="parser.Isolate" requiresKeys="id,find" requiresValues="regexid,string"/>
			<Rule type="parser.Replace" requiresKeys="id,find,replace,regex" requiresValues="parser.regexid,string,string,boolean"/>
			<Rule type="parser.Split" requiresKeys="id,find,n,regex" requiresValues="regexid,string,int,boolean"/>+
			<Rule type="xmlhandler.Trace" requiresKeys="id,nodes,defVal" requiresValues="regexid,intstringarray,string"/>
			<Rule type="jsonhandler.Trace" requiresKeys="id,path,defVal" requiresValues="regexid,intstringarray,string"/>
		</Parser>
	</Parsers>
	<Constants>
		<Constant requiresKeys="name,id,order" requiresValues="regexname,regexid,regexidlist">
			<Values>
				<Value requiresKeys="isKey,backRefernce,useHeader,id" requiresValues="boolean,boolean,boolean,regexid"/>
			</Values>
		</Constant>
	</Constants>
</InterfaceOasis>
